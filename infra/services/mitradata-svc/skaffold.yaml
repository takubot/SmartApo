# infra/services/mitradata-svc/skaffold.yaml
apiVersion: skaffold/v4beta7
kind: Config
metadata:
  name: mitradata-svc
# ────────────────────────────────────────────────────────────
# Profiles
# ────────────────────────────────────────────────────────────
profiles:
  - name: dev
    build:
      artifacts:
        - image: mitradata-svc
          context: ..
          docker:
            dockerfile: python/domain/mitradata-svc/dockerfile
    manifests:
      rawYaml:
        - dev-cloudrun.yaml
    deploy:
      cloudrun:
        projectid: "{{ .ENV.PROJECT_ID }}"
        region:    "{{ .ENV.REGION }}"
  - name: stg
    build:
      artifacts:
        - image: mitradata-svc
          context: ..
          docker:
            dockerfile: python/domain/mitradata-svc/dockerfile
    manifests:
      rawYaml:
        - stg-cloudrun.yaml
    deploy:
      cloudrun:
        projectid: "{{ .ENV.PROJECT_ID }}"
        region:    "{{ .ENV.REGION }}"
  - name: prod
    build:
      artifacts:
        - image: mitradata-svc
          context: ..
          docker:
            dockerfile: python/domain/mitradata-svc/dockerfile
    manifests:
      rawYaml:
        - prod-cloudrun.yaml
    deploy:
      cloudrun:
        projectid: "{{ .ENV.PROJECT_ID }}"
        region:    "{{ .ENV.REGION }}"