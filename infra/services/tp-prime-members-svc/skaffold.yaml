# infra/services/tp-prime-members-svc/skaffold.yaml
apiVersion: skaffold/v4beta7
kind: Config
metadata:
  name: tp-prime-members-svc
# ────────────────────────────────────────────────────────────
# Profiles
# ────────────────────────────────────────────────────────────
profiles:
  - name: dev
    build:
      artifacts:
        - image: tp-prime-members-svc
          context: ..
          docker:
            dockerfile: python/domain/tp-prime-members-svc/dockerfile
    manifests:
      rawYaml:
        - dev-cloudrun.yaml
    deploy:
      cloudrun:
        projectid: "{{ .ENV.PROJECT_ID }}"
        region:    "{{ .ENV.REGION }}"
  - name: stg
    build:
      artifacts:
        - image: tp-prime-members-svc
          context: ..
          docker:
            dockerfile: python/domain/tp-prime-members-svc/dockerfile
    manifests:
      rawYaml:
        - stg-cloudrun.yaml
    deploy:
      cloudrun:
        projectid: "{{ .ENV.PROJECT_ID }}"
        region:    "{{ .ENV.REGION }}"
  - name: prod
    build:
      artifacts:
        - image: tp-prime-members-svc
          context: ..
          docker:
            dockerfile: python/domain/tp-prime-members-svc/dockerfile
    manifests:
      rawYaml:
        - prod-cloudrun.yaml
    deploy:
      cloudrun:
        projectid: "{{ .ENV.PROJECT_ID }}"
        region:    "{{ .ENV.REGION }}"