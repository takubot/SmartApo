# infra/services/miyakoya-svc/skaffold.yaml
apiVersion: skaffold/v4beta7
kind: Config
metadata:
  name: miyakoya-svc
# ────────────────────────────────────────────────────────────
# Profiles
# ────────────────────────────────────────────────────────────
profiles:
  - name: dev
    build:
      artifacts:
        - image: miyakoya-svc
          context: ..
          docker:
            dockerfile: python/domain/miyakoya-svc/dockerfile
    manifests:
      rawYaml:
        - dev-cloudrun.yaml
    deploy:
      cloudrun:
        projectid: "{{ .ENV.PROJECT_ID }}"
        region:    "{{ .ENV.REGION }}"
  - name: stg
    build:
      artifacts:
        - image: miyakoya-svc
          context: ..
          docker:
            dockerfile: python/domain/miyakoya-svc/dockerfile
    manifests:
      rawYaml:
        - stg-cloudrun.yaml
    deploy:
      cloudrun:
        projectid: "{{ .ENV.PROJECT_ID }}"
        region:    "{{ .ENV.REGION }}"
  - name: prod
    build:
      artifacts:
        - image: miyakoya-svc
          context: ..
          docker:
            dockerfile: python/domain/miyakoya-svc/dockerfile
    manifests:
      rawYaml:
        - prod-cloudrun.yaml
    deploy:
      cloudrun:
        projectid: "{{ .ENV.PROJECT_ID }}"
        region:    "{{ .ENV.REGION }}"